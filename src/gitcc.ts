/*
 * @fileoverview Exports all public APIs of gitcc. Injected into user custom validator scripts.
 */

export { invalid, valid, warning } from "@/result"
export { splitCommitMessage } from "@/commit"
